import figlet from 'figlet';
const helpFootFont = figlet.textSync("ixyz", {
  font: "Standard",
  horizontalLayout: "full",
  verticalLayout: "full",
  whitespaceBreak: true,
})
export default helpFootFont;